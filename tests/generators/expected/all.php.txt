<?php

function main()
{
    $n = (int) trim(fgets(STDIN));
    $s = 0;
    $count = 0;
    for ($i = 1; $i <= $n; $i++) {
        if ($i < 5) {
            for ($j = 0; $j < $i; $j = $j + 1) {
                switch ($j) {
                    case 0:
                        continue 2;
                    case 1:
                    case 2:
                        while ($s < 100) {
                            $s = $s + $j;
                            if ($s > 50) {
                                break;
                            }
                        }
                        break;
                    default:
                        do {
                            $s = $s - 1;
                            if ($s < 10) {
                                continue;
                            }
                            echo "s = ", $s, PHP_EOL;
                        } while ($s > 0);
                }
            }
        } else {
            foreach ($items as $x) {
                show($x);
                if ($x > $i) {
                    return $x;
                }
            }
        }
    }
    echo "s = ", $s, ", count = ", $count, PHP_EOL;
}

main();
