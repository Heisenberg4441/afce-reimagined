program Algorithm;

var
  s, k, x: Integer;

begin
  s := 0;
  for k := 0 to 2 do
  begin
    s = s + k;
    s = s + 1;
  end;
  x := 1 + 2;
  if (x > 1) and (x < 5) then
  begin
    WriteLn(s, x);
  end;
end.
