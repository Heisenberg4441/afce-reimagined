алг Алгоритм
нач
  цел a, i, x, x_i, j
  нц пока a < b
    если a > 100 то
      выход
    все
    если a < 0 то
      | continue: переход к следующему шагу цикла (не поддерживается)
    все
    a := a + 1
  кц
  нц
    a := a + 1
    если a > 5 то
      | continue: переход к следующему шагу цикла (не поддерживается)
    все
    если a > 50 то
      выход
    все
  кц при a >= b
  нц для i от 1 до 10
    если i > 5 то
      выход
    иначе
      | continue: переход к следующему шагу цикла (не поддерживается)
    все
  кц
  i := 0
  нц пока i < 10
    если i > 8 то
      выход
    все
    если i > 5 то
      | continue: переход к следующему шагу цикла (не поддерживается)
    все
    вывод i, нс
    i := i + 1
  кц
  нц для x_i от 1 до длин(items)
    x := items[x_i]
    если x < 0 то
      | continue: переход к следующему шагу цикла (не поддерживается)
    иначе
      выход
    все
  кц
  j := 0
  нц пока j < 3
    выбор
      при j = 0:
        | continue: переход к следующему шагу цикла (не поддерживается)
      при j = 1:
        вывод "one", нс
      иначе
        вывод j, нс
    все
    j := j + 1
  кц
  если a > b то
    выход
  иначе
    выход
  все
кон
