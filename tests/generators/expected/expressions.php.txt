<?php

function main()
{
    $ok = $a > 0 && $b != 0 || !$done;
    if (($x % 2 == 0 && $y < 10) || $flag == true) {
        echo "yes && !no", $x != $y, PHP_EOL;
    }
    while (!($i >= $n) && $p != NULL) {
        $i = $i + 1;
    }
    do {
        $finished = false;
    } while (!$finished);
}

main();
