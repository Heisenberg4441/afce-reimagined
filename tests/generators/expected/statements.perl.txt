#!/usr/bin/env perl
use strict;
use warnings;
use utf8;
use open qw(:std :encoding(UTF-8));

sub main {
    my ($s, $k, $x);
    $s = 0;
    for my $k (0..2) {
        $s = $s + $k;
        $s = $s + 1;
    }
    $x = 1 + 2;
    if ($x > 1 && $x < 5) {
        print($s, $x, "\n");
    }
}

main();
