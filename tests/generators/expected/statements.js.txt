function main() {
    let s, x;
    s = 0;
    for (let k = 0; k <= 2; k++) {
        s = s + k;
        s = s + 1;
    }
    x = 1 + 2;
    if (x > 1 && x < 5) {
        console.log(`${s}${x}`);
    }
}

main();
