#include <stdbool.h>
#include <stdio.h>

int main(void) {
    int n, k;
    n = 10;
    while (n > 0) {
        n = n - 3;
    }
    do {
        n = n + 1;
    } while (n < 5);
    for (int i = 1; i <= 10; i++) {
        printf("%d\n", i);
    }
    for (int j = 0; j <= n - 1; j++) {
        printf("%d\n", j);
    }
    for (k = 0; k < n; k = k + 2) {
        printf("%d\n", k);
    }
    for (;;) {
        work();
        break;
    }
    for (size_t x_i = 0; x_i < sizeof(items) / sizeof(items[0]); x_i++) {
        int x = items[x_i];
        printf("%d\n", x);
    }
    while (n > 100) {
    }
}
